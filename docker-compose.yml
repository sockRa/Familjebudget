services:
  app:
    build: .
    ports:
      - "3000:3000"
    volumes:
      - budget-data:/app/data
    environment:
      - NODE_ENV=production
      - DB_PATH=/app/data/budget.db
    restart: unless-stopped

volumes:
  budget-data:
